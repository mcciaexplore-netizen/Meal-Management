# Verification record

Local verification for the independent applications, employee registration, Gmail delivery, and automatic sender was performed on 2026-09-09. Automated tests did not contact MySQL, AWS, or a real email provider. Earlier process tests used temporary loopback listeners with fictional configuration and explicitly blocked database connections. Separately approved operational checks established local queue access, one Gmail-accepted message with a committed `SENT` status, and cancellation of the remaining queued messages. A later approved read-only check found zero queued emails. The automatic-sender implementation did not change the private `.env` or start live delivery; both sending flags remain disabled pending approval for ongoing delivery. The application is not production ready while the integration and deployment checks below remain unverified.

| Check | Result |
| --- | --- |
| Python unit and API suite | Latest automatic-sender update: 626 passed, no skips |
| Included FastAPI/HTTPX API cases | Passed using injected services, including admin-only email settings and scanner isolation |
| Frontend Node tests | Latest automatic-sender update: 141 passed, no skips, including automatic/disabled/manual email modes, preview permissions, department resolution, inline photo capture, camera cleanup, dialog ownership, application separation, and missing-marker recovery |
| Gmail adapter, worker, and CLI tests | Included above: 20 Gmail/configuration cases, 19 worker cases, and 7 CLI cases; SMTP and database dependencies were injected substitutes |
| Automatic sender and lifecycle | Included above: 24 dispatcher tests, 20 lifecycle/configuration/API tests, and 3 SES SDK retry/timeout regressions, all offline |
| Frontend asset build | Passed; separate `dist/admin` and `dist/scanner` directories |
| Built asset serving through FastAPI | Each root page and its own assets served successfully; the other application's JavaScript returned 404 |
| Python dependency consistency | Earlier `pip check` passed; dependencies were not changed or installed in this phase |
| Migration plan | All five migration files loaded and checksummed without a database connection; none were changed and no sixth migration was added |
| Independent process startup | 2 subprocess tests passed with database calls forbidden: separate PIDs/ports, printed URLs, assets, admin shutdown without scanner shutdown, and occupied-port handling |
| Startup configuration | 9 unit tests included above cover default ports, loopback binding, foreground launch, configuration and asset failures, no proxy trust, and safe error output |
| Authored Python source policy | No comments or docstrings, checked by the unit suite |
| MySQL integration discovery | 83 tests explicitly skipped with both connection/schema opt-in flags set to zero, including 11 delivery and dispatcher scenarios |
| Visual browser and physical camera checks | Not performed; laptop and phone checklists provided separately |

API tests cover login, logout, cookie protection, CSRF and origin validation, server permissions, both QR categories, caller and payload binding, committed-result replay, invalid input, safe transaction failure responses, report filters, pagination, and private photo/QR/email routes. Threaded API tests use a locked fake service; these establish the HTTP contract, not InnoDB behavior. Service tests separately exercise SQL parameters, commit ordering, rollback paths, QR lifecycle, idle sessions, and receipt processing with isolated transaction substitutes.

New coverage includes disjoint API routes and static directories, per-application origins, cross-port mutation rejection, scanner cookies that cannot authenticate administration, and database-backed lookup of the last browser-owned request when a marker is missing. Bootstrap and liveness do not query MySQL. Existing suites cover both QR kinds, visitor validation, commit failures, legacy compatibility, shared identities, rate limits, and seed preservation. No fixture or default scanner records were inserted into the application database by these tests.

Frontend tests exercise configured links between applications, separate builds, removal of login and station selectors, minimal public payloads, independent scanner transport, employee acceptance, the four-field visitor form, request retries, correctable HTTP 422 responses, delayed requests, and stable pending markers. A missing marker triggers a browser-owned server lookup before scanning; malformed or failed recovery blocks startup. Prior staff markers remain untouched. Stored markers exclude credentials and visitor contact details. Simulated media devices do not establish that real phone camera workflows work.

The registration update adds the eight requested facility department choices while preserving other active departments. Tests cover existing IDs, inactive departments, creation on form submission through the existing admin API, concurrent department creation, and retry after a lost response. Camera tests use simulated devices and image encoding to check front-camera preference, capture and upload selection, preview, retake, cancellation, private local previews, and cleanup when leaving or replacing a dialog. A delayed employee-detail response cannot replace a newer registration dialog. Both application asset bundles were rebuilt successfully. No department records or employee photos were inserted during this verification; actual camera capture and saving against MySQL remain manual checks.

The Gmail update adds verified TLS, secret-safe App Password configuration, PNG QR attachments, and an explicitly approved one-message CLI sender. Tests cover committed claims, simultaneous send commands, stale QR or employee state, reserved test recipients, definite SMTP rejection, uncertain body/final-response failures, safe error output, and commit acknowledgement failures. Replaying a sent entry makes no provider call; failed or uncertain entries are never automatically resent. The existing schema is reused. Fake SMTP acceptance is not proof that a real Google account will authenticate or that a message will reach an inbox. MySQL delivery integration cases also use fake provider calls when enabled; they were not executed here.

Automatic delivery runs only in the admin lifespan when both real-sending and auto-send flags are enabled. Tests verify committed queued-only selection, bounded batches, polling backoff, global authentication-failure halt, graceful shutdown, disabled startup with no database access, and scanner isolation. Multiple dispatchers reuse the same durable claim logic. SES SDK retries are disabled to prevent an internal provider retry from bypassing that claim. A previous single Gmail send verifies account authentication and provider acceptance only; it does not establish automatic-loop behavior, recipient inbox delivery, or concurrent MySQL correctness. The automatic loop remains unverified against a live server.

The local tests use installed password hashing, QR rendering, encryption, and image-processing dependencies. AWS adapter tests use fake clients and do not verify IAM, S3, or SES. Photo tests use temporary private storage. Process tests exercise actual Uvicorn listeners on temporary localhost ports, while API/service behavior is tested with injected substitutes. These process checks prove independent liveness, not successful MySQL meal recording while the administrator is stopped.

Two new guarded MySQL integration tests construct independent application factories and service instances against the same disposable database. They cover employee reuse, direct master meals, committed-result recovery after scanner recreation, missing-marker lookup, dashboard history and totals, UTC timestamps, all visitor fields, scanner system attribution, and API/credential isolation. They were discovered but not executed. Actual end-to-end database recording and dashboard visibility therefore remain unverified.

The installed Starlette version emits a deprecation warning for its HTTPX TestClient adapter. The HTTPX tests passed; no additional test-client dependency was installed. Recheck framework and test-client compatibility when updating dependencies.

## Remaining verification and release steps

- Confirm the intended local development target before applying pending migrations or inserting fixtures. The currently inspected target is `127.0.0.1:3306/meal_management`, with development mode, Gmail configured, and real and automatic sending disabled.
- Run all 83 MySQL integration cases in an independently approved disposable database namespace, including email delivery claims, automatic dispatchers and recovery, independent-application/report tests, shared-scanner browser isolation, immutable defaults, concurrent claims, blocked scanner login/admin roles, changed visitor submissions, malformed retry recovery, direct master constraints, reports, seed concurrency, and rollback behavior.
- Verify migrations 001 through 005 on a fresh MySQL 8.4 database and the reviewed upgrade on a backed-up copy of an existing database. The live migration ledger was not inspected during this email update. No new email migration is required. Finish or reconcile pending servings, stop the running application before a migration, then restart it after successful migration.
- Verify actual cookie sessions and rate limits, employee registration, private photos, email previews, employee scans, master visitor forms, and dashboard reports together against MySQL. Stop only the administrator process, record employee and visitor meals, restart administration, and verify its refreshed history.
- Review desktop and phone layouts in a browser and test real camera scanning on target devices. Approve network exposure and HTTPS configuration separately before phone access; no such changes were made in this phase.
- Choose EC2-hosted MySQL or RDS, application hosting, DNS/TLS, networking, secret distribution, backups, and recovery procedures.
- Separately approve enabling ongoing automatic delivery and delivery-status updates in the development database, then verify registration/resend delivery, pending-queue recovery after restart, and shutdown behavior. Verify durable claims and acknowledgements against MySQL. Implement and verify managed retries, delivery monitoring, provider quota management, and bounce handling before unattended production delivery. Configure private production storage; AWS permissions and SES remain separate optional provider checks.
- Approve AWS access and deployment separately, then verify health checks, proxy trust, process supervision, monitoring, backup restoration, and release rollback.

See [local setup](../README.md), [development fixtures and device testing](development-testing.md), [API contracts](api.md), and [deployment instructions](deployment.md). Skipped integration tests and unavailable browser checks are not counted as passes.
